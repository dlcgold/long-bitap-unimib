#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <math.h>

char* charge_file(char*);
int* charge_u_char(char*, char);
int charge_u_int(char*, char);
int array_bitn(int, char*);
int* array_bit(int, char*);
