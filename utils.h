#ifndef UTILS_H
#define UTILS_H

#include <stdio.h>
#include <string.h>
#include <limits.h>
#include <malloc.h>
#include <math.h>
#include <assert.h>
#include <stdlib.h>

char* substr(const char*, int, int);
int* bitap(char*, char*);
void bitapLong(char*, char*);
char* read_file(char*);

#endif

